# API Configuration
# Base URL for the backend API. Leave empty to use relative /api paths.
# Example for local development: http://localhost:8000
VITE_API_URL=

# Demo Mode
# Set to 'true' to enable Mock Service Worker (MSW) for development without a backend.
# Set to 'false' or leave empty to connect to the real API.
VITE_DEMO_MODE=false

# Optional: Provide a Bearer token for API authentication.
# If not set, the app will try to use a token from localStorage.
VITE_AUTH_TOKEN=
